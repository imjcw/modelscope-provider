## 初始化与提交计划

### 现状
- 当前仓库路径：`/mnt/d/workspace/third/provider`
- 当前分支：`batch-task-5-8`
- 远程仓库：尚未配置
- 已变更/新增文件：
  - `M .env`：本地环境/密钥文件，已在 `.gitignore` 中忽略；当前版本仍被 Git 追踪，不建议提交
  - `M api/routes.py`：OpenAI 兼容 `/v1/chat/completions`、流式响应、错误处理
  - `M main.py`：FastAPI lifespan 初始化、服务注入
  - `M models/alias_resolver.py`：模型别名解析逻辑
  - `?? openai_proxy.py`、`?? server.py`：未追踪文件，含重复/替代实现和硬编码密钥风险

### 执行步骤
1. 添加远程仓库：
   ```bash
   git remote add origin git@github.com:imjcw/modelscope-provider.git
   ```
   若 `origin` 已存在，则更新 URL：
   ```bash
   git remote set-url origin git@github.com:imjcw/modelscope-provider.git
   ```

2. 保护本地配置不被提交：
   - 重置 `.env` 的暂存/工作区变更，避免把真实密钥/配置提交
   - `.env` 本身已被 `.gitignore` 忽略，但因历史追踪，需用 `git rm --cached .env` 或恢复暂存状态后再提交

3. 提交核心代码变更：
   - `api/routes.py`
   - `main.py`
   - `models/alias_resolver.py`

   提交信息采用：
   ```text
   feat: implement OpenAI-compatible chat proxy endpoint and service initialization
   ```

4. 暂不提交 `openai_proxy.py` 和 `server.py`：
   - 原因是它们看起来像替代实现，且存在硬编码 API key 风险
   - 如需纳入，请先确认并清理敏感信息

5. 推送到远程，并设置 upstream：
   ```bash
   git push -u origin batch-task-5-8
   ```

### 需要你确认/注意
- SSH 能否访问 `git@github.com:imjcw/modelscope-provider.git`
- 是否允许我把 `openai_proxy.py` / `server.py` 排除在本次提交外
- `.env` 会按安全做法从本次提交中排除
